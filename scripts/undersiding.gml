if undersides
    {
    if place_meeting(x,y,enemy) with instance_place(x,y,enemy) underside()
            
    if place_meeting(x,y,shellmove)
        {
        with instance_place(x,y,shellmove) underside()
        underside()
        }      
    if place_meeting(x,y,shell) with instance_place(x,y,shell) underside()
    }
